fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
pgvector>=0.3
python-docx>=1.1
python-multipart>=0.0.9
lxml>=5.0

# Provider decisions (locked in):
google-generativeai>=0.8      # Gemini embeddings (text-embedding-004) + generation
llama-parse>=0.5              # external PDF parsing API (client sign-off required)

# PyMuPDF does double duty: it extracts embedded images from uploaded PDFs
# (app/services/document_images.py) and rasterises the NCC/ABCB corpus's SVG
# figures to PNG so a vision model can read them. Chosen over cairosvg and
# svglib/reportlab because it needs no native cairo build, which is the
# difference between "pip install" working and not working on Windows.
# cairosvg is used instead when present, but is not required.
pymupdf>=1.24

# Test/dev tooling only -- not needed to run the app itself.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient.TestClient
ruff>=0.6
